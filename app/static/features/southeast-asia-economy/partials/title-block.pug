mixin title-block(post, i)

  section.title-block
    .background
    .flex.w-title-block
      .col-text
        h1!= post.title
        p.caption!= post.bioLong

      .col-image
        img(src=`/features/${slug}/img/authors/${post.href}.jpg`)